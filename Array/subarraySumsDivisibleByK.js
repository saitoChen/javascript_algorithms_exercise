// https://leetcode.cn/problems/subarray-sums-divisible-by-k/

const subarraysDivByK = (nums, k) => {
  if (!nums || nums.length === 0) return 0
  const result = []

  const traverse = (nums, path, sum) => {}

  
};